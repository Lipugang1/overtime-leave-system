# 2026-06-07 工作记录

## 启动系统修复

### Turbopack 中文路径崩溃
- Next.js 16.1.1 的 Turbopack 在中文路径下会 panic
- 错误日志：`reading file E:\扣子调休管理系统\nul` → Windows 保留设备名导致 `os error 1`
- `turbopack: false` 配置在 Next.js 16 中不被支持（需要 object 类型）
- `--no-turbopack` 命令行参数在 Next.js 16 中不存在
- **解决方案**：使用 `next build && next start` 生产模式启动（跳过 Turbopack dev server）

### 密码验证格式不匹配
- 数据库中 admin 密码是 **bcrypt** 格式（`$2b$10$...`）
- `src/lib/password.ts` 的 `verifyPassword()` 只支持 **PBKDF2** 格式（`iterations.salt.hash`）
- bcrypt hash 用 `.` 分割后只有 2 段，`parts.length !== 3` → 返回 false
- **修复**：`verifyPassword` 现在同时支持 bcrypt 和 PBKDF2 格式

### 启动方式（最终方案）
```bash
# 先构建（用 Turbopack 构建 OK，只有 dev server panic）
C:/Users/administered/.workbuddy/binaries/node/versions/22.22.2/node.exe node_modules/next/dist/bin/next build

# 再启动生产模式
C:/Users/administered/.workbuddy/binaries/node/versions/22.22.2/node.exe node_modules/next/dist/bin/next start --port 3000
```

## GitHub 代码推送
- 仓库：https://github.com/Lipugang1/overtime-leave-system
- 用 PAT (ghp_TIBwRXTe2Qy7...) 推送成功
- 默认分支已改为 master

## 部署状态
- Vercel CLI 在沙箱中无法运行（路径权限问题）
- 已提供手动 Vercel 部署指南（通过网页 https://vercel.com/import）
- 需配置 4 个环境变量：COZE_SUPABASE_URL、COZE_SUPABASE_ANON_KEY、COZE_SUPABASE_SERVICE_ROLE_KEY、JWT_SECRET
