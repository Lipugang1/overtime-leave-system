# 2026-06-06 工作日志

## 完成事项
- 安装了项目依赖（pnpm install，使用 debug 模式绕过 store 数据库权限问题）
- 配置 .env.local 文件，填入 Supabase 项目凭据
  - Project URL: https://pnqfvfhqekgroliztnuo.supabase.co
  - 已配置 anon key 和 service_role key
- 生成数据库初始化 SQL（scripts/init-db.sql），包含所有表结构和初始管理员账号（admin/admin123）
- 成功启动 Next.js 开发服务器（端口 3000，PID 35252），返回 200
- 数据库已通过 Supabase SQL Editor 手动执行 init-db.sql 完成初始化（Success. No rows returned）
- 管理员账号已创建：admin / admin123
- 服务器重启完成（端口 3000，PID 17604），HTTP 200 正常
- 数据库密码设为 lipugang461023，但直连端口 5432/6543 被网络封锁

## 已知问题
- 无法从沙箱环境直连 PostgreSQL（端口 5432/6543 不通）
- 无法通过 REST API 或 Management API 自动执行 DDL
- Supabase SQL Editor 需手动执行（浏览器扩展冲突时可用无痕模式）

## 启动命令
- 终结旧进程：`taskkill /PID <PID> /F`
- 启动：`"C:\Users\administered\.workbuddy\binaries\node\versions\22.22.2\node.exe" node_modules/next/dist/bin/next dev --port 3000`（必须用 Node 22）

## 修复"登记加班/调休"提交无反应问题

### 根因分析
**前后端不一致的审批人校验**：
- 后端 `overtime/route.ts` 第 48-51 行：`!isAdmin(user)` 时硬性要求必须有 `selected_level1_approver_id`，否则返回 400 `"请选择审批人"`
- 前端：当 `loadApprovers` 返回空数组（系统中无对应岗位人员，如没有经理/副经理/经理助理）时，审批人选择框不显示，`isFormValid()` 也判定为有效
- 结果：用户填完表单点击"确认登记"，后端 400 被 `alert()` 拦截，但用户无法解决（没有审批人可选）

### 修复文件
1. `src/app/api/overtime/route.ts`：非管理员审批人校验改为"仅当系统中存在可选审批人时"才强制要求
2. `src/app/api/leave/route.ts`：同上修复
3. `src/app/(main)/overtime/page.tsx`：
   - 新增 `approverRule` 状态存储审批规则
   - `loadApprovers` 改为同时捕获 rule 信息
   - 添加"系统暂无可选审批人"提示，告知用户可以提交但需后期补充审批人

### 验证结果
- 非管理员(李普刚/06007516)无审批人提交加班：HTTP 200，记录创建成功
- 前端页面重启后正常加载

## 全面测试与 FK 修复

### FK 约束名称修复
- 根本原因：代码中 9 个 API 路由文件的 Supabase `.select()` 使用硬编码 FK 名称 `_employees_id_fk`，
  但 PostgreSQL 自动生成的默认名称是 `_fkey`
- 修复：全部 9 个文件中的 `_employees_id_fk` 替换为 `_fkey`
- 验证：`grep -r "_employees_id_fk" src/` 返回零结果，确认无遗漏

### API 全面测试结果（admin 登录，9 个端点全部 200 OK）
| API | 状态 | 备注 |
|-----|------|------|
| /api/dashboard | 200 ✅ | 返回 2 名员工、余额、审批计数 |
| /api/overtime | 200 ✅ | 空数据（正常，暂无记录） |
| /api/leave | 200 ✅ | 空数据（正常，暂无记录） |
| /api/approval | 200 ✅ | 空数据（正常，无需审批） |
| /api/employees | 200 ✅ | 返回 admin + 李普刚(06007516) |
| /api/balances | 200 ✅ | 返回工时余额汇总 |
| /api/approvers | 200 ✅ | 审批规则正确返回 |
| /api/overtime/export | 200 ✅ | CSV 导出正常 |
| /api/leave/export | 200 ✅ | CSV 导出正常 |

### 前端页面验证
- 首页 (/) → 正常加载
- 加班管理 (/overtime) → 正常加载（之前崩溃，已修复）
- 审批中心 (/approval) → 正常加载（之前崩溃，已修复）
- 服务器：端口 3000，Node 22，稳定运行

## 修复手写签名保存失败

### 根因
签名 API (`api/auth/signature/route.ts`) 使用 `coze-coding-dev-sdk` 的 `S3Storage` 上传图片到对象存储，
依赖环境变量 `COZE_BUCKET_ENDPOINT_URL` 和 `COZE_BUCKET_NAME`。本地环境未配置这些变量，导致
`storage.uploadFile()` 抛出异常，前端表现为"保存签名"按钮无反应。

### 修复方案
重写签名 API，添加本地文件系统回退模式：
- 当 `COZE_BUCKET_ENDPOINT_URL` 或 `COZE_BUCKET_NAME` 未配置时，自动退回到 `public/signatures/` 目录存储
- POST 上传：将签名图片写入 `public/signatures/{user_id}_{timestamp}.{ext}`
- GET 获取：返回 `/{file_path}`（public 目录下的文件可直接通过 URL 访问）
- DELETE 删除：同时删除本地文件和数据库记录
- 同时兼容 Coze S3 模式（生产环境）

### 验证
- 上传签名：`POST /api/auth/signature` → 200，`{"signature_key":"signatures/xxx.png"}`
- 获取签名：`GET /api/auth/signature` → 200，`{"signature_url":"/signatures/xxx.png","has_signature":true}`
- HTTP 访问签名图片：200 OK
- 删除签名：`DELETE /api/auth/signature` → 200

## 修复申请单导出（DOCX）失败

### 根因
加班/调休的 DOCX 申请单导出（`export-docx/route.ts`）在两个地方依赖 `coze-coding-dev-sdk` 的 S3Storage：
1. `getSignatureImage()` — 读取签名图片嵌入 DOCX
2. 最终导出 — 上传生成的 .docx 到 S3 并返回预签名下载 URL

本地环境未配置 `COZE_BUCKET_ENDPOINT_URL`，两处均抛异常 → 前端报 "Storage endpoint not configured"。

### 修复
两个文件（`overtime/export-docx/route.ts`、`leave/export-docx/route.ts`）统一修复：

**1. getSignatureImage() 本地回退**
- 有 Coze S3 配置 → 走 S3 `readFile`
- 无配置 → 从 `public/` 目录读取签名文件（按 `signature_key` 路径）

**2. 导出文件上传本地回退**
- 有 Coze S3 配置 → 上传到 S3 + 生成预签名 URL
- 无配置 → 写入 `public/exports/overtime/` 或 `public/exports/leave/`，返回 `/exports/...` 直接 URL

### 验证
- 加班申请单 DOCX 导出测试：200 OK
- 文件生成：`public/exports/overtime/加班登记单_系统管理员.docx`
- download_url 返回本地路径，浏览器可直接下载

## Vercel 自动部署成功
- **问题链路**：`main` 分支缺少 `src/` 源码 → Vercel 始终部署旧提交 → pnpm 兼容性问题 → `vercel.json` schema 校验失败
- **最终方案**：放弃 Git 分支同步，改用 `vercel deploy --prod` 本地构建后直接上传产物
- **构建结果**：Next.js 16.1.1 + Turbopack，43s 构建完成，所有 37 个路由正常
- **GitHub 同步**：138 个源文件已通过 API 推送到 `main` 分支（138 次 commit），Vercel 免费部署配额（100次/天）耗尽，需等明天配额重置后自动部署
- **代码库**：https://github.com/Lipugang1/overtime-leave-system/tree/main
- **生产地址**：https://overtime-leave-system.vercel.app
- **关键修复**：
  - `vercel.json` 去掉 `nodeVersion` 字段（Vercel schema 不支持）
  - `pnpm install` 改为 `npm install`（`pnpm` 在 Vercel 构建环境有 `ERR_INVALID_THIS` 兼容问题）
  - `next.config.js` 用纯 JS（避免 TypeScript 转译依赖）
- **Vercel Token**：`vcp_8cMl1...`（用户已提供，已用于本次部署）

## EdgeOne Pages 部署

- **项目名称**：overtime-leave-system
- **部署方式**：从 GitHub 导入（Provider: Github），不支持 CLI 直传
- **环境变量已配置**（via `edgeone pages env set`）：
  - `COZE_SUPABASE_URL` = `https://pnqfvfhqekgroliztnuo.supabase.co`
  - `COZE_SUPABASE_ANON_KEY`、`COZE_SUPABASE_SERVICE_ROLE_KEY`、`JWT_SECRET`
- **关键修复**：
  - 移除 `pnpm-lock.yaml` → 重命名为 `.bak`（EdgeOne 构建环境 pnpm 会报 "unable to open database file"）
  - 用 `npm install` + `npm run build` 替代
  - 构建成功：Next.js 16.1.1 + Turbopack，48s，35 条边缘函数路由
- **CLI 部署失败原因**：EdgeOne Pages 项目是 GitHub Provider 类型，不支持 CLI 直传文件夹
- **已推送到 GitHub main 分支**（force push），EdgeOne Pages 应自动触发重新部署
- **前期问题**：注册/登录报 "Internal Server Error" → 根因是 EdgeOne Pages 未配置 Supabase 环境变量
- **Token**：用户已提供 EdgeOne API Token

## EdgeOne Pages 注册/登录失败修复

### 根因
`src/storage/database/supabase-client.ts` 包含 Coze 平台专用代码，在 EdgeOne Pages serverless 环境崩溃：
- `import { execSync } from 'child_process'` + `execSync('python3 ...')` → EdgeOne 无 Python 子进程支持
- `import { getReportBuffer, createWrappedFetch } from 'coze-coding-dev-sdk'` → Coze 专用 SDK，非通用运行时兼容

### 修复
重写 `supabase-client.ts`，移除 `child_process`、`coze-coding-dev-sdk` 及 Python 回退逻辑，
仅保留 `dotenv.config()` 和 `process.env` 读取。EdgeOne Pages 已在构建/运行时注入环境变量。

### 提交
`dcf5329` — fix: remove Coze SDK and child_process from supabase-client for EdgeOne Pages
`edcb682` — fix: add runtime='nodejs' to all 26 API routes for EdgeOne Pages

## EdgeOne Pages runtime 修复

### 根因2
所有 26 个 API 路由未设置 `export const runtime = 'nodejs'`。
EdgeOne Pages 默认以 **edge runtime** 运行 serverless 函数，
edge runtime 不支持 `bcryptjs`（依赖 Node.js `crypto`）、`jose`（依赖 Web Crypto 的 Node.js 实现）、文件系统等。

### 修复
批量添加 `export const runtime = 'nodejs';` 到所有 26 个 `src/app/api/**/route.ts` 文件。
